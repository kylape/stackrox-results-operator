---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: nodevulnerabilities.security.stackrox.io
spec:
  group: security.stackrox.io
  names:
    kind: NodeVulnerability
    listKind: NodeVulnerabilityList
    plural: nodevulnerabilities
    singular: nodevulnerability
  scope: Cluster
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.nodeName
      name: Node
      type: string
    - jsonPath: .spec.osImage
      name: OS
      type: string
    - jsonPath: .spec.summary.critical.total
      name: Critical
      type: integer
    - jsonPath: .spec.summary.high.total
      name: High
      type: integer
    - jsonPath: .spec.summary.total
      name: Total
      type: integer
    - jsonPath: .spec.summary.fixableTotal
      name: Fixable
      type: integer
    - jsonPath: .spec.scanTime
      name: Scanned
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: NodeVulnerability is the Schema for the nodevulnerabilities API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: NodeVulnerabilitySpec defines the desired state of NodeVulnerability
            properties:
              architecture:
                type: string
              containerRuntime:
                type: string
              cves:
                description: Top CVEs (limited to prevent resource bloat)
                items:
                  description: CVE describes a vulnerability
                  properties:
                    component:
                      description: Component describes a vulnerable software component
                      properties:
                        location:
                          type: string
                        name:
                          type: string
                        version:
                          type: string
                      type: object
                    cve:
                      pattern: ^CVE-[0-9]{4}-[0-9]+$
                      type: string
                    cvss:
                      description: CVSS score (0-10)
                      type: string
                    cvssV3:
                      description: CVSSv3 contains CVSS v3 scoring
                      properties:
                        score:
                          type: string
                        vector:
                          type: string
                      required:
                      - score
                      - vector
                      type: object
                    discoveredInImage:
                      format: date-time
                      type: string
                    epss:
                      description: EPSS contains Exploit Prediction Scoring System
                        data
                      properties:
                        percentile:
                          type: string
                        score:
                          type: string
                      required:
                      - percentile
                      - score
                      type: object
                    fixable:
                      type: boolean
                    fixedBy:
                      description: Version that fixes this CVE
                      type: string
                    link:
                      type: string
                    published:
                      format: date-time
                      type: string
                    severity:
                      enum:
                      - CRITICAL
                      - HIGH
                      - MEDIUM
                      - LOW
                      type: string
                    state:
                      enum:
                      - OBSERVED
                      - DEFERRED
                      - FALSE_POSITIVE
                      type: string
                    summary:
                      type: string
                  required:
                  - cve
                  - severity
                  type: object
                  x-kubernetes-validations:
                  - rule: "true"
                maxItems: 100
                type: array
              kernelVersion:
                type: string
              kubeletVersion:
                type: string
              nodeName:
                description: Node identification
                type: string
              osImage:
                type: string
              scanTime:
                description: When the node was last scanned
                format: date-time
                type: string
              summary:
                description: CVE summary by severity
                properties:
                  critical:
                    description: SeverityCount counts vulnerabilities at a severity
                      level
                    properties:
                      fixable:
                        type: integer
                      total:
                        type: integer
                    required:
                    - fixable
                    - total
                    type: object
                  fixableTotal:
                    type: integer
                  high:
                    description: SeverityCount counts vulnerabilities at a severity
                      level
                    properties:
                      fixable:
                        type: integer
                      total:
                        type: integer
                    required:
                    - fixable
                    - total
                    type: object
                  low:
                    description: SeverityCount counts vulnerabilities at a severity
                      level
                    properties:
                      fixable:
                        type: integer
                      total:
                        type: integer
                    required:
                    - fixable
                    - total
                    type: object
                  medium:
                    description: SeverityCount counts vulnerabilities at a severity
                      level
                    properties:
                      fixable:
                        type: integer
                      total:
                        type: integer
                    required:
                    - fixable
                    - total
                    type: object
                  total:
                    type: integer
                required:
                - fixableTotal
                - total
                type: object
            required:
            - nodeName
            - osImage
            - scanTime
            - summary
            type: object
          status:
            description: NodeVulnerabilityStatus defines the observed state of NodeVulnerability
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
