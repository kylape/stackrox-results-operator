---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.19.0
  name: alerts.results.stackrox.io
spec:
  group: results.stackrox.io
  names:
    kind: Alert
    listKind: AlertList
    plural: alerts
    singular: alert
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .status.policyName
      name: Policy
      type: string
    - jsonPath: .status.policySeverity
      name: Severity
      type: string
    - jsonPath: .status.entity.type
      name: Entity Type
      type: string
    - jsonPath: .status.entity.name
      name: Entity
      type: string
    - jsonPath: .status.state
      name: State
      type: string
    - jsonPath: .status.lifecycleStage
      name: Lifecycle
      type: string
    - jsonPath: .status.time
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: Alert is the Schema for the alerts API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: spec defines the desired state of Alert
            type: object
          status:
            description: status defines the observed state of Alert
            properties:
              conditionMessage:
                description: Current condition message
                type: string
              enforcementAction:
                description: Enforcement action taken
                type: string
              enforcementCount:
                description: Number of times enforcement was applied
                type: integer
              entity:
                description: Entity that violated the policy
                properties:
                  clusterId:
                    description: Cluster ID
                    type: string
                  clusterName:
                    description: Cluster name
                    type: string
                  id:
                    description: Entity ID
                    type: string
                  name:
                    description: Entity name (deployment name, image name, resource
                      name, etc.)
                    type: string
                  namespace:
                    description: Namespace (for namespaced entities)
                    type: string
                  resourceType:
                    description: Resource type (for Resource entities - e.g., "Secret",
                      "ConfigMap")
                    type: string
                  type:
                    description: Type of entity (Deployment, Pod, Image, Node, Resource)
                    type: string
                type: object
              firstOccurred:
                description: When this alert was first triggered
                format: date-time
                type: string
              lifecycleStage:
                description: Lifecycle stage when the violation was detected
                enum:
                - DEPLOY
                - RUNTIME
                type: string
              policyCategories:
                items:
                  type: string
                type: array
              policyDescription:
                type: string
              policyId:
                description: Policy information
                type: string
              policyName:
                type: string
              policySeverity:
                enum:
                - CRITICAL
                - HIGH
                - MEDIUM
                - LOW
                - UNKNOWN
                type: string
              resolutionNote:
                description: Note explaining resolution
                type: string
              resolvedAt:
                description: When the alert was resolved
                format: date-time
                type: string
              state:
                description: Current state of the alert
                enum:
                - ACTIVE
                - RESOLVED
                - ATTEMPTED
                type: string
              time:
                description: When this alert occurred
                format: date-time
                type: string
              violations:
                description: Violation details
                items:
                  description: Violation describes a specific policy violation
                  properties:
                    keyValueAttrs:
                      description: Additional violation attributes
                      items:
                        description: KeyValueAttr is a key-value pair for violation
                          details
                        properties:
                          key:
                            type: string
                          value:
                            type: string
                        required:
                        - key
                        - value
                        type: object
                      type: array
                    message:
                      description: Human-readable violation message
                      type: string
                    type:
                      description: Type of violation
                      type: string
                  type: object
                type: array
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
